# MRA game assets & binaries (not part of this tooling, large/binary)
*.256
*.zip
*.exe
*.EXE
*.MR0
*.HLP
*.SEC
*.gif
*.wav
*.mid
*.bmp
*.dll
*.ico

# Extracted / bundled game data (regenerable from the zips/exe)
DUMP/
IHELP/
IHELP2/
IHELP3/
__MACOSX/
scb/
WINMRA/
_maps_unzip/
_mapsall_unzip/
_winmra_dump/
SEC mapping/
Tekken/pics/

# Stray PNGs (analysis renders) stay ignored, but the tool's rendered tiles
# ARE tracked so the map tool works when cloned / hosted.
*.png
!_render/tiles/
!_render/tiles/*.png

# Live-compiled maps (regenerated from Supabase by the build_*.py compilers)
world_map_built.json
clusters_built.json

# More binaries / IDA / backups / game data
*.i64
*.bak
*.TXX
*.TOC
*.000
*.CFG
*.INI
*.pyc
__pycache__/
PAL256
TITLEC
MR.TOC

# Game text dumps (we keep MAPS.TXT only)
MANUAL.TXT
HELP.TXT
NPQ.TXT
NPQPRIZE.TXT
MRADUMP.TXT
README.TXT
ogn_protocol_log.txt
_commit_msg.txt
*.log

# Other dev's reference map editor (study only, not ours to ship)
Reference/

# OS / editor cruft
Thumbs.db
.DS_Store
